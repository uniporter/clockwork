abstract class LocaleData {
    static const LOCALE_DATA = "Locale Data";
    static final Map<String, Locale> data = Map();

    static String _dataPath = "";

    static set setDataPath(String dataPath) {
        _dataPath = dataPath;
    }

    static load(String localeName) {
        
    }
}

class Locale {

}